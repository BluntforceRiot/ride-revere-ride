{
  "name": "ride-revere-ride",
  "description": "v0.9.2 browser-demo Phaser score-attack game.",
  "version": "0.9.2",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "vite --config vite/config.dev.mjs",
    "typecheck": "tsc --noEmit",
    "build": "vite build --config vite/config.prod.mjs",
    "preview": "vite preview --config vite/config.prod.mjs"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.4.3"
  },
  "dependencies": {
    "phaser": "4.0.0",
    "terser": "^5.39.0"
  }
}
